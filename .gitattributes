# The last matching pattern wins, so the generic rule goes first.
* text=auto

# Shell scripts and systemd units run on Linux — they must keep LF endings even
# when the repo is edited on Windows, or bash fails with "bad interpreter".
*.sh text eol=lf
*.service text eol=lf
*.timer text eol=lf
